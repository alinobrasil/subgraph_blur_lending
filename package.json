{
  "name": "blur_lending",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ blend",
    "create-local": "graph create --node http://localhost:8020/ blend",
    "remove-local": "graph remove --node http://localhost:8020/ blend",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 blend",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}